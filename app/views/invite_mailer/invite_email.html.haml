:markdown
  # You are invited to an OPENKITCHEN

  #{@host.email} has invited you.

  #{@invitation.event.name}

= link_to "Goto invitation", token_invitations_url(:id => @invitation.token), :id => "invitation_link"
